# TurnSignal

Vehicle recon tracker for independent dealerships.

## What this is

This is the real, code-level version of TurnSignal, connected to the same
Supabase backend your Lovable project uses. No Lovable credits required to
keep building from here — changes happen directly in this code.

## Getting it live (no coding experience required)

1. **Create a GitHub repository.** Go to github.com, click "New repository,"
   name it `turnsignal`, and create it.
2. **Upload this code.** On the new repo's page, click "uploading an existing
   file" and drag in everything from this folder (you can drag the whole
   folder in most browsers).
3. **Connect it to Vercel.** Go to vercel.com, click "Add New Project," and
   select the `turnsignal` repository you just created.
4. **Add your environment variables in Vercel.** In the project setup screen,
   add two environment variables:
   - `VITE_SUPABASE_URL` → `https://ofsiaawfohekhpfmyqaf.supabase.co`
   - `VITE_SUPABASE_PUBLISHABLE_KEY` → (the long key from your .env file)
5. **Click Deploy.** Vercel builds and hosts it automatically. You'll get a
   live URL in about a minute.

## Running it on your own computer (optional)

If you want to preview changes before deploying, you'll need Node.js
installed (nodejs.org, the LTS version). Then, in a terminal, inside this
folder:

```
npm install
npm run dev
```

That starts it at http://localhost:5173

## Status

- [x] Login (Supabase email/password auth)
- [x] Supabase connection wired to your real project
- [x] Main kanban board, reading/writing real data from your `vehicles` table
- [x] Sidebar boards (Loaners, Body Shop, Waiting on Title, Auction/Wholesale)
- [x] VIN decode (NHTSA API)
- [x] Stock number and mileage are both optional now — not every vehicle has them right away (requires `supabase/mileage_optional_and_color.sql`)
- [x] Optional color field, manual entry (VINs don't encode color)
- [x] Full editing — pencil icon on each card opens the same form pre-filled, saves changes instead of creating a duplicate
- [x] Delete a vehicle — from the edit modal (pencil icon), with confirmation
- [x] Completed cards collapse to a slim row, Planner-style — tap the name to peek at full details again
- [x] Loaners board now has two columns: Loaners and Service Loaners
- [x] Reorder cards within a column by dragging — uses the `position` column that already existed in the schema
- [x] Boards and columns are now editable, Owner-only — rename, add, or delete any board or column per dealership (requires `supabase/boards_table_and_audit_migration.sql`). A "⚙ Manage" link appears next to the board tabs only when logged in as Owner.
- [x] Card title restyled — stock number is now bold and slightly larger, leading the rest of the title
- [x] Tap the day-count badge itself to open the stage timeline — no separate clock icon needed anymore
- [x] Per-dealership aging colors — Owner can set how many days until yellow/red for each dealership individually (requires `supabase/thresholds_and_names_migration.sql`). "🎨 Aging colors" appears next to "⚙ Manage" for Owner only.
- [x] Edit or delete any note after the fact — no more being stuck with a typo or a note on the wrong vehicle
- [x] First and last names — collected at signup, editable anytime via the new "Name" button. Notes, added-by, and completed-by all show the real name once it's set, falling back to the old email-based short name for anyone who hasn't set one yet.
- [x] Dealership roles — Manager, Sales, Service, Detail, Photo (requires `supabase/dealership_roles_migration.sql`). Owner can designate the first Manager for any dealership via the new "👤 Roles" button; once someone is a Manager, they can assign roles to others in their own dealership too — including at invite time. Roles are optional and don't restrict access yet (that's planned for later); right now they're just labels.
- [x] Notes modal auto-closes after adding a note instead of requiring a manual Close
- [x] Click anywhere on a card's title or details to edit it — the separate pencil button is gone
- [x] Board scroll resets to the start every time you switch tabs, instead of staying wherever it was left
- [x] Photos on each card — tap the photo icon next to Notes to view, add, or delete photos (requires `supabase/photos_and_damage_migration.sql`, sets up Supabase Storage for the first time)
- [x] Damage flag — a checkbox in the add/edit form; when checked, a bold "⚠ DAMAGE" badge shows directly on the card. Honest note: this is a visible on-board alert, not a push/email notification to a manager's phone — that's bigger infrastructure tied to the notifications system already on the roadmap.
- [x] Dealership groups — Owner can group dealerships together (e.g. "Johnson Motors") from the dealership picker, assigning each one via a dropdown on its card (requires `supabase/dealership_groups_migration.sql`). Anyone with the Manager role sees a new "🏢 Switch store" button letting them jump to any other store in their group with full access — same model as how Owner Mode already works, not view-only. A Manager only keeps Manager-only controls (Roles button, etc.) on their own home store, not on siblings they're visiting. Every new permission here is additive — nothing existing was modified, specifically to avoid repeating the earlier lockout.
- [x] Owner now automatically gets every feature a role gets, including the store switcher — fixed the gap where it only checked for Manager
- [x] Dealer list reorganized into three columns: Dealer Groups (one row per group, click to pick a specific store inside it), Dealers (standalone, ungrouped), and Paused
- [x] Analytics page — Owner and Manager only, "📊 Analytics" button right next to the dealership name. Current count and average time per stage, turn time (average/fastest/slowest) from Inbound to Price for Lot, bottleneck stage, longest-aging active vehicle, damage and overdue-loaner counts, all filterable by Today/Week/Month/Quarter/Year or a custom date range. Built from the existing `stage_history` and `vehicles` data — no new tables needed. Stats computation is kept separate from rendering specifically so a future chart view can reuse it directly.
- [x] Bottleneck stage recalculated — now based on current backlog (count × how long each vehicle has been sitting there right now), not just historical average duration. A single car stuck a week now correctly outweighs many cars moving quickly through a high-volume stage. Shared aging-threshold logic moved into `lib/aging.ts` so VehicleCard and Analytics can never drift apart.
- [x] Completed vehicles now collapse into a single "Completed (N)" toggle per column, Planner-style, instead of stacking up individually — tap to expand/collapse the whole group. Column header count now reflects active vehicles only, matching how Planner's main count works too.
- [x] Completed slim rows: the whole row is now tappable to pop back open (not just the narrow name text), with a clear chevron hint, and there's now an explicit way to collapse it back down again too
- [x] Mobile keyboard covering inputs in Notes and the Add/Edit vehicle form — added a scroll-into-view nudge once the keyboard finishes animating in. Honest caveat: this is a real iOS Safari viewport quirk, not something fully fixable from CSS alone — it should genuinely improve once this is wrapped as a native app shell later.
- [x] Dealerships and dealership groups can now be renamed anytime — editable name field on each, both in the dealer list and inside a group's member list. Uses permissions that already existed, no new SQL needed.
- [x] Owner profile deletion protection — the actual bug behind the recent account lockout. Dealership deletion no longer touches Owner-role profiles at all (the app code now explicitly excludes them), and a database trigger now makes it physically impossible for any Owner profile to be deleted by any code path, present or future, including raw SQL. Owner profiles no longer need a dealership_id at all (requires `supabase/protect_owner_profile_migration.sql`).
- [x] VIN scanner now validates against the real NHTSA checksum (49 CFR Part 565) — every North American VIN has a built-in check digit at position 9. The scanner pulls every possible 17-character window out of what it read and prefers whichever one actually passes the checksum, across both OCR passes, instead of just taking the first 17-character run it finds. If nothing passes, it says so clearly rather than silently accepting a likely-wrong guess.
- [x] Analytics headline renamed to "Turn Rate (Service → Price for Lot)" and recalculated to start from when a vehicle actually enters Service, not from Inbound — Inbound wait time (pickup/transit, out of the dealership's control) no longer counts toward it
- [x] "Aging red right now" no longer counts the Loaners board — those vehicles are already on the lot, out with customers or managers, not stuck in recon
- [x] Loaners board cards no longer show green/yellow/red color coding at all (still tracks and displays days, just neutral gray, same treatment Inbound already had) — shared aging logic now considers board, not just stage, so this can never drift out of sync between the cards and Analytics
- [x] Carrying cost (holding cost) — every card shows a running dollar total, using separate per-day rates for new vs used (requires `supabase/carrying_cost_migration.sql`). Rates live in Analytics (Owner/Manager only to edit, visible to everyone), with a live "Total carrying cost across active inventory" stat alongside it. A "New vehicle" checkbox on the add/edit form defaults smartly off the model year (current year or one year back) but is always manually overridable. Bonus consistency fix: Aging Colors is now Owner+Manager too, matching how every other dealership-level setting already works.
- [x] Carrying cost refined per clarified rules (requires `supabase/completed_at_migration.sql`): New vehicles' clock now starts when they actually enter Service (using the existing `recon_started_at`), not when the card is added — transit/inbound time never counts for new. Used still starts at card creation. Both freeze at the real moment of completion (a new `completed_at` timestamp) instead of climbing forever. Turn Rate's end point also moved to match — it's now "completed while in Price for Lot," not just "arrived in Price for Lot," since a vehicle can sit there a while before recon work is genuinely done. Card display simplified to just the dollar amount on its own line, no "holding cost" label, slightly more prominent than the surrounding details.
- [x] Carrying cost excludes the Loaners board entirely — those are vehicles already on the lot, not recon inventory accruing holding cost. Same exclusion pattern already used for aging colors.
- [x] Multiple photos can be selected and uploaded at once — removed the camera-only restriction so tapping "Add photos" shows the full native picker (camera or library), and the library option supports multi-select. Tap any photo to view it full-screen, large; native pinch-to-zoom works there too since nothing in the app blocks browser-level zooming.
- [x] Bottleneck stage redefined again, per direct feedback: back to historical average time to get through a stage (same number as the "Avg. time" column in the table below), not current backlog. This also fixes the original complaint that Bottleneck never changed with the date range — since it's now driven by the same historically-filtered data as everything else, it does.
- [x] Team member removal — Owner and Manager can now remove a teammate's dealership access from "Team Roles," with a confirmation prompt first. Owner rows are hidden from removal entirely (protected at the database level too, independent of this UI), and you can't remove your own row by accident. A removed person sees a clear "your access has been removed" message instead of a cryptic error if they're still logged in. Manager permissions here (and for Aging Colors) now apply across their whole group, not just their home store, matching how full board access on sibling stores already worked — a deliberate change from an earlier, more restrictive assumption.
- [x] Fixed the "popped open" completed-card view being washed out and hard to read — it was incorrectly inheriting the same dimmed opacity used for the collapsed slim row. Full detail view is now fully legible regardless of completion status; only the collapsed row stays intentionally faded.
- [x] In-app notifications (no email) — a 🔔 bell in the header with an unread badge. Cards can now be assigned to a specific teammate via a dropdown in the add/edit form (requires `supabase/notifications_and_team_delete_migration.sql`); the assignee gets a notification the moment a card is newly assigned to them, and the card itself shows "👤 Assigned: [name]" so anyone glancing at it knows who's responsible. Honest scoping note: tapping a notification marks it read and shows what happened, but doesn't yet deep-link to the actual card — the message includes enough detail (stock number, vehicle) to find it manually. Architecture leaves room for more notification triggers later (e.g. damage flagged, overdue) without a schema change.
- [x] Automatic role notifications on stage entry (no new SQL — reuses the notifications table from above) — Service, Detail (covers both Detail Backlog and Active Detail), Photo, and Manager each get notified the moment a vehicle lands in their relevant stage, whether it got there by being added directly or moved from elsewhere. Message reads like "Gunner moved 924123-2021 Chevy Tahoe Z71 to Service." Wired into the single shared move/create functions every part of the app already goes through, so it fires correctly from the dropdown, drag-and-drop, or direct creation — not duplicated per-UI. Someone doesn't get notified about their own action if they happen to hold the matching role themselves.
- [x] VIN scanner overhauled around the actual root cause of bad reads — it was forcing a single-line interpretation onto photos that, the moment framing wasn't pixel-perfect, contained slivers of the row above or below the VIN, jumbling two lines into one corrupted string. Now treats the photo as a real multi-line page (PSM.AUTO, with PSM.SPARSE_TEXT as a fallback pass) and scans each detected row independently, so the system finds the right row itself instead of requiring exact single-line alignment. The capture guide box is now wide enough for the whole sticker, not a thin single-line strip. Hard guard added per direct request: if no full, real 17-character run of valid VIN characters is found anywhere, the field is left blank with a clear message — never populated with a 3-4 character fragment that only looks like a real answer. Manual VIN typing is now also forced uppercase, matching scanned VINs.
- [x] Analytics: "Longest aging, active now" no longer pulls from the Loaners board (or Inbound) — those vehicles are already on the lot, not stuck in recon, matching every other aging exclusion already on this page.
- [x] Analytics: switching the date range tab now actually pulls fresh data instead of just recomputing from whatever was loaded when the page first opened, plus a manual "🔄 Refresh" button for pulling current numbers anytime without closing and reopening. Honest scoping note: this is not full real-time (the page won't update itself the instant someone elsewhere moves a card) — that's genuine new infrastructure (Supabase Realtime) worth a deliberate decision before building, not something to bolt on quietly.
- [x] Analytics: three new stats — "Carrying cost added this period" (the cost specifically accrued during the selected date range, distinct from the live running total, computed off the real overlap between each vehicle's accrual window and the range), damage rate shown as a percentage alongside the raw count, and a "Completions this period" leaderboard by who completed the most vehicles (a simple count, not a stricter productivity claim, since marking complete isn't always exactly the same person who did all the work).
- [x] Title status — a small icon on every card (turn-signal arrow for "have title," yield triangle for "POA," no-entry circle for "waiting on title," a neutral dashed circle when not yet set) replacing the manual title-tracking spreadsheet (requires `supabase/title_status_migration.sql`). Only Owner and Manager can tap it to change it — enforced at the database level with a real trigger, not just hidden in the UI, mirroring the same protection pattern already used for dealership roles. Everyone else sees the icon (with a hover/accessible label, never visible text on the card itself, per direct request) but it's not interactive for them. Deliberately state-agnostic — labeled just "POA," no state prefix, so it works for a dealer in any state.
- [x] Fixed the desktop horizontal scrollbar not working when the browser window is narrowed below the board's full width — same underlying cause as the earlier VIN-scanner camera bug, just on the opposite axis: a flex item's default refusal to shrink below its content's natural width, so the board kept growing past the visible screen instead of letting its own internal scrollbar take over. Added the explicit `min-w-0` override that lets it actually shrink and scroll correctly.
- [x] Analytics: added "Added in this period," "Aging red right now" (a count, distinct from the single worst-case "longest aging"), and two clearly-marked placeholder cards reserved for floorplan cost data once that calculation is ready
- [x] Visible, always-on horizontal scrollbar for the board area — fixes desktop windows that aren't maximized having no obvious way to scroll
- [x] Completed vehicles now sink to the bottom of their column instead of sitting wherever their position value happens to place them
- [x] Stage history now shows duration down to the minute (e.g. "45 min", "3h 20m") instead of rounding to whole days, and timestamps include time-of-day
- [x] Waiting on Title now uses the exact same aging color thresholds as every other stage — its extra leniency is gone
- [x] Column headers are bolder and slightly larger so they stand out more
- [x] Header no longer overflows/freezes on phones — collapsed Invite/Name/Password/Dealer-list/Sign-out into a "⋯" dropdown menu, kept Switch Store and Analytics exactly where they were, and made the header wrap instead of ever pushing the page wider than the screen
- [x] Brightened low-contrast light-gray text on dark backgrounds (header captions/buttons, Analytics page header and headline card) — added a proper `mist` color specifically for this, since `steel` was too close to the dark background it sat on
- [x] Move a vehicle to any board, not just within the current one — the dropdown now groups every destination by board
- [x] Self-service signup — invite a teammate by email, they create their own password and get auto-linked to your dealership (requires `supabase/invite_system_migration.sql`)
- [x] Self-service password change — anyone logged in can change their own password, including accounts you created for them manually
- [x] Live cross-column drag preview — cards now visually shift into the column you're hovering over, not just on drop
- [x] Card title leads with stock number (e.g. "923567-2017 GMC Sierra 1500 SLT"), no longer duplicated below
- [x] Pause or permanently delete a dealership from Owner Mode (requires `supabase/deactivate_delete_dealership_migration.sql`) — pausing blocks their access without touching their data; delete requires typing the dealership's exact name to confirm
- [x] Per-column add button
- [x] Dealership name in header
- [x] Move vehicles between stages — drag-and-drop (via a small handle on each card, works with mouse and touch) plus the dropdown as a guaranteed-to-work fallback
- [x] Loaner overdue badge (red, based on loaner_return_date)
- [x] Title aging alert (Waiting on Title uses a 10-day threshold instead of the standard 5-day one)
- [x] Total recon time tracking — the badge shows continuous total time since leaving Inbound/Trade-In, never resets on a stage move (requires `supabase/stage_history_migration.sql`)
- [x] Per-vehicle stage timeline — tap the clock icon on a card to see exactly how long it spent in each stage
- [x] Notes — multiple timestamped notes per vehicle, latest shown on the card, click to see full history (requires `supabase/notes_and_completed_migration.sql`)
- [x] Completed checkbox on each card, Planner-style (same migration above)
- [x] Owner mode — requires running `supabase/owner_mode_migration.sql` first (see below)
- [x] Add new dealerships directly from Owner Mode (no SQL needed for this part)

## Adding a user to any dealership (manual, for now)

Creating someone's actual login still has to be done manually — see "Owner mode setup" below for why. Once a dealership exists (built into the app now), add a person to it like this:

1. Lovable → Cloud icon → Users → create the new user (email + password, enable auto-confirm if available)
2. Copy that new user's UUID
3. Run this in the SQL editor, swapping in the real values:

```sql
INSERT INTO profiles (id, dealership_id, email, role)
VALUES (
  'PASTE_NEW_USER_UUID_HERE',
  (SELECT id FROM dealerships WHERE name = 'Johnson Motors Menomonie'),
  'their_email_here',
  'dealer'
);
```

That looks up the dealership by name, so this same snippet works for any dealership you've created — just change the name and the person's info.
- [x] VIN scan — confirmed working end to end. The real bug turned out to be a crop-coordinate mismatch: the camera preview displays with `object-cover` (cropping the raw feed to fill the screen), but the photo capture was cropping based on the raw, uncropped frame instead of the on-screen displayed area — so the green guide box and the actual captured pixels were quietly misaligned, consistently losing the left side of the VIN. Fixed by computing the crop relative to the video's actual displayed bounding box. Combined with the NHTSA checksum validation above, this is now a reliable path, not a parked feature.
- [x] Fixed hard-to-tap Close buttons across every modal in the app (13 files) — the previous buttons were sized to their text content only, well under the ~44px minimum touch target Apple/Google both recommend, which is exactly why taps were being missed on a phone. New shared `ModalCloseButton` component gives every modal a real, consistent hit area plus `touch-manipulation` to remove the tap-delay/double-tap-zoom ambiguity that can make a tap feel unresponsive even when it registers.
- [x] Search — a 🔍 icon in the board's nav bar opens a live search across every board and stage in the dealership (not just the currently-active tab), matching stock number, VIN, year, make, model, or trim. Selecting a result switches to the right board tab if needed, scrolls the card into view, and gives it a temporary blue highlight ring for a few seconds. A result inside a collapsed "Completed" section auto-expands that section so the result is actually visible, not hidden.
- [x] Scan-to-move — the existing floating scan button is now smarter: scan a VIN, and if it matches a vehicle already on the board, a picker shows every board/stage as a tap target to move it directly — no re-typing, no going back to a computer. If the VIN doesn't match anything yet, it hands off to Add Vehicle with the VIN already confirmed and decoded (defaulting to Inbound/Trade-In, since an unrecognized scan is almost always a fresh arrival) rather than making the person scan or type it a second time.
- [x] Fixed the edit-card modal's Close button becoming unreachable on longer forms/with the keyboard open — the header (with Close) was inside the same scrolling area as the whole form, so scrolling or the keyboard pushed it out of view with nothing pinned to get back to. Restructured so the header is now a fixed, non-scrolling part of the modal and only the form body scrolls — Close is always visible regardless of scroll position.
- [x] Fixed the search highlight ring being clipped on its sides instead of showing all the way around — cards had zero horizontal padding in their scroll container, so the ring (which paints just outside the card's edge) had nowhere to render before hitting the container's own clipping boundary. There's also a real CSS spec quirk at play: setting `overflow-y: auto` silently forces `overflow-x` to clip too, even when only vertical scrolling was intended. Fixed by giving the scroll container padding plus a matching negative margin, so the ring has room without changing the visible layout.
- [x] Fixed scan-to-move failing to recognize a vehicle already on the board — the match required an exact character-for-character VIN comparison, so even one character read differently between two separate photos of the same sticker (which can happen with any OCR, even reliable cloud OCR) silently broke the match and fell through to "add new." Added a tolerant fallback: if no exact match, check for a vehicle whose VIN is off by only 1-2 characters, and if there's exactly one such close candidate, ask for a quick confirmation ("Close match — is this it?") before proceeding — never silently guesses wrong, never gives up over a single misread character either.
- [x] Fixed the VIN scanner hanging indefinitely on "Reading VIN…" with no way out — the cloud OCR request had no timeout, so a slow or hung connection just waited forever instead of ever falling back to the on-device scanner. Added a 12-second timeout that automatically falls through to the local fallback if the cloud call doesn't come back in time.
- [x] Fixed the Close button being unreachable specifically in Safari on iPhone (confirmed working in Chrome on the same phone/deploy, which ruled out a stale-deploy issue and pointed at something Safari-specific) — Safari's own collapsing toolbar and its handling of the on-screen keyboard both change the actual visible viewport without the layout math behind plain `vh` units or `position: fixed` updating to match, a real, well-documented Safari-only quirk (Chrome for iOS handles its own browser chrome differently, even on the same underlying engine). Switched every modal's height cap to `dvh` (dynamic viewport height), which tracks the real visible area, with the original `vh` value kept as an automatic fallback for anything that doesn't support `dvh`. Applied consistently across all 9 modals using this pattern, not just the one reported.
- [x] Column reordering — up/down arrows next to each column in "⚙ Manage" let you reorder columns within a board (e.g. re-adding a deleted Detail Backlog wherever you want it, not just at the end). Order is just array order under the hood, so reordering is a simple swap-and-save. Extended the whole Manage boards/columns screen from Owner-only to Owner+Manager, including at the database level (requires `supabase/manager_board_management_migration.sql`) — Managers could previously view boards but not write to them at all.
- [x] Carrying cost exception: a vehicle on the Loaners board that's still marked "Waiting on title" now keeps accruing carrying cost, instead of freezing the moment it's moved to Loaners. Most loaners genuinely shouldn't count (they're already on the lot, out with someone) but a loaner still waiting on its title is a real, ongoing cost, not a completed sale — fixed consistently everywhere carrying cost is calculated (the card, the live total, the period total, and the new/used averages), not just one of them.
- [x] Moved the "← Main Board" button in Analytics from the far right to right next to the dealership name, matching the "📊 Analytics" button's placement on the main board — no more reaching across the screen to get back.
- [x] Fixed scroll-snap not actually engaging on the board — `scroll-snap-type` was declared on a non-scrolling child div instead of the actual scrolling element, which per the CSS Scroll Snap spec means it likely wasn't taking effect correctly at all (explaining the "free-flowing" feel instead of a clean snap). Moved it onto the real scroll container. Also changed column width on mobile from a deliberate 86%-width "peek at the next column" to filling the full available width, so swiping now shows exactly one column at a time, Planner-style, with a clean snap between them — no more sliver of the next column bleeding in.
- [x] Fixed the floating scan button visually overlapping card-level modals (Notes, Timeline, Photos, etc.) instead of staying safely behind them. Rather than relying on z-index layering alone, the button now fully hides itself whenever any card's own modal is open, tracked via a small counter fed by every card on the board — guarantees the two can never visually collide regardless of the exact cause.
- [x] Adding a vehicle now scrolls to and highlights the exact new card afterward — switching to the right board tab first if needed — instead of leaving you wherever the board happened to be scrolled. Reuses the same scroll-and-highlight mechanism already built for search results, so a freshly added vehicle gets the identical "here it is" treatment a search match does.
- [x] Fixed the actual root cause behind cards popping open "broken" specifically in Safari (confirmed working in Chrome on the same phone, which was the key clue) — when a card's modal renders from deep inside the board's own scrolling containers (the horizontal board scroll, then a column's vertical scroll), Safari has a long-documented bug where a fixed, cover-the-whole-screen overlay loses that ability and gets visually trapped inside its scrolling ancestor instead of covering the screen — exactly matching the "two screens overlapping" symptom, immediately and consistently, not just after interacting with the form. Chrome doesn't have this bug, which is why it only showed up in Safari. Fixed by portaling all five of a card's own modals (Notes, Timeline, Photos, Title Status, Edit) straight to `document.body`, so none of them are DOM descendants of any scrolling container anymore, in any browser.
- [x] Fixed the notification panel running off the left edge of the screen on mobile — it was positioned relative to the bell icon itself, which works fine when the icon sits at the true right edge of the screen, but the header wraps to two lines on a narrow phone and the bell ends up at the *left* of its own line instead, pushing the panel off-screen. Switched to fixed positioning with safe viewport margins instead, so it can never overflow regardless of where the bell ends up sitting.
- [x] Executive Summary added to the top of Analytics — plain-language insight bullets (turn rate with a trend arrow vs. the immediately preceding period of equal length, needs-attention count, bottleneck stage, estimated carrying cost) built entirely from stats already computed elsewhere on the page, so it can never quietly disagree with the detailed cards below it. Nothing existing was removed or reordered — this sits above everything else as a 30-second-glance layer.
- [x] "Needs Attention" panel — lists every vehicle currently past the dealership's aging threshold, worst first, with how many days over. Tapping one closes Analytics and jumps straight to that card on the board (switching store/board tab first if needed), reusing the same scroll-and-highlight treatment search results and newly-added vehicles already get. Bridged through Dashboard.tsx (the shared parent of Analytics and the board, since they're siblings, not nested) rather than a deep prop-drill or unreliable remount timing.
- [x] Both new Analytics sections also included in PDF export, consistent with every other addition to this page.
- [x] Major Analytics expansion, per a detailed rule-based spec (deliberately no AI/LLM anywhere — every score is transparent arithmetic a manager can verify by hand): **Priority Score** (0-100, capped point system across 6 factors — days over aging target, carrying cost vs. average, days in current stage, total recon time, waiting on title, loaner overdue — new `lib/priorityScoring.ts`) powers **Today's Priorities** (top 5 highest-scored active vehicles, each showing its score, the exact contributing reasons, and one recommended action, clickable straight to the card). **Dealership Performance Score** (0-100, five 20-point categories — Turn Rate, Vehicle Aging, Stage Efficiency, Completion Rate, Carrying Cost — deliberately no letter grade per direct instruction; a category without enough data yet is left out of both the score and its max, never guessed at). **Stage Health Dashboard** (0-100 health score per Main Board stage with a green/yellow/red indicator, current average, vehicle counts, and a plain-language recommendation). **Lost Money Dashboard** (existing carrying cost figures plus a new "carrying cost avoided vs. previous period" comparison and a narrative "$X per additional day" sentence — no gross profit estimate, only known rate data). **Recommendations Engine** (predefined rule conditions only, each recommendation naming the exact data that triggered it). All of it built from data already being fetched — nothing new tracked — and every "not enough history yet" case is handled explicitly rather than showing a misleading number. Nothing existing on the page was removed or reordered; all of this sits above the original cards.
- [x] Activity Feed (vehicles added, stage changes, notes, photos, completions, filterable by user/date) was part of the same spec but deliberately not built this round — it needs new cross-table history queries and its own filtering UI, genuinely different scope from the scoring/dashboard work above. Flagged as the natural next build.
- [x] Reworked the big Analytics expansion per direct feedback that it had gotten too long and cluttered: Loaners board now excluded from Today's Priorities entirely (unless waiting on title — mirrors the exact carrying-cost exception). "Dealership Performance Score" replaced with a simpler, more honest **Turn Rate Score** — compares the current period's turn rate against this dealership's own all-time historical average (not a 5-category blend with several judgment calls baked in), shown as one compact number + one sentence right next to the Turn Rate hero. "Carrying cost added this period" tile removed (redundant — the rate is something the dealership already configured); replaced with a genuinely useful "avoided/added vs. previous period" comparison in that same spot. Removed the "every additional day costs $X" narrative sentence (redundant with the already-known configured rate) and the whole separate "Lost Money Dashboard" heading — the one piece worth keeping (cost avoided vs. previous period) now lives directly in the existing carrying-cost tiles instead of a new section. Removed "Completions this period" entirely — not useful when one person is completing nearly everything. **Today's Priorities** and **Recommendations** are now compact one-line summary tiles on the main page (showing a count and a preview) that open a dedicated drill-in modal with the full detail when tapped — Stage Health and Needs Attention stay inline as before, since neither was flagged as too long. Both new drill-ins (`TodaysPrioritiesModal.tsx`, `RecommendationsModal.tsx`) are rendered outside the page's scrolling body, same lesson learned from the earlier Safari fixed-position bug.
- [x] A true per-manager customizable dashboard (choosing which tiles show at all) is a good next step beyond today's rework, but needs its own persistence (somewhere to save each manager's preferences) — flagged as a real next build rather than half-implemented here.
- [x] Consolidation pass on the Analytics expansion, in response to direct feedback that it still felt like a dense report to decipher rather than a scannable "here's what to do today" page. Found and removed three real overlaps rather than just reorganizing: (1) Needs Attention and Today's Priorities were showing largely the same vehicles two different ways — removed Needs Attention entirely, since Today's Priorities already covers it with more detail (a score + the exact reason, including "days over the aging target" when that's why it's there). (2) Turn Rate Score, the Executive Summary's turn-rate line, and the Turn Rate hero card were all independently describing the same trend — this is also why the score "wasn't changing": comparing a consistently-performing store against its own all-time average naturally stays near 100 most of the time, so the number wasn't adding anything the sentence above it didn't already say. Removed the separate score card. (3) Stage Health's per-stage recommendation, the Executive Summary's bottleneck line, and the Recommendations tile were all independently saying some version of "Service is slow" — removed the bottleneck line and the whole separate Recommendations section; Stage Health is now the one place stage-level advice lives. The two genuinely non-redundant Recommendations conditions (title paperwork piling up, carrying cost trending up) moved into Executive Summary as occasional extra lines that only appear when actually true. Executive Summary itself is shorter and terser as a result — down to 2-4 short lines instead of 4 full sentences every time.

## Owner mode setup (one-time)

1. Open `supabase/owner_mode_migration.sql` in this folder
2. Replace `YOUR_EMAIL_HERE` with your actual login email
3. Run the whole file in Supabase's SQL editor (Lovable → Cloud icon → Database → SQL editor)
4. Sign out and back in on the live app — you'll now see a dealership picker instead of going straight to a board

## On drag-and-drop

Vehicles move between stages using a "Move to…" dropdown on each card instead of
dragging. This was a deliberate choice: native drag-and-drop gestures are
unreliable on mobile browsers, and this tool is meant to be used on a phone
in a parking lot. A dropdown always works the same way regardless of device.
